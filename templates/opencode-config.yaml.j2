# OpenCode CLI configuration
# Rendered by the Go create command from the configured secrets file.

api_key: "{{ .OPENAI_API_KEY }}"
{{- if .OPENCODE_API_KEY }}
opencode_api_key: "{{ .OPENCODE_API_KEY }}"
{{- end }}

git_config:
  user_name: "{{ .GIT_USER_NAME }}"
  user_email: "{{ .GIT_USER_EMAIL }}"
